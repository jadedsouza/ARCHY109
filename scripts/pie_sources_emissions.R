# plot a pie chart for the distribution of different sources of emissions
library("plotly")
library("dplyr")

pie_sources_emissions <- function(emissions, info, country, year) {
print(paste("girl", emissions, "a", info, country, year))
}
